[package]
name = "frontend"
version = "0.1.0"
edition = "2024"

[dependencies]
shared = { path = "../shared" }
leptos = { version = "0.8", features = ["csr"] }
leptos_router = "0.8"
leptos-fetch = "0.4"
gloo-net = { version = "0.7", features = ["http"] }
console_error_panic_hook = "0.1.7"
